import { getResources } from "@/lib/api"; export const metadata={title:"Resources"};
export default async function Resources(){const r=await getResources({per_page:50}); return <><section className="page-hero section"><p className="kicker">The reference shelf</p><h1>Resources</h1><p className="lead">Books, papers, tools, equipment, courses, and useful references that support better observation and clinical reasoning.</p></section><section className="section"><div className="resource-grid resources-page">{r.data?.map((x,i)=><a href={x.url||'#'} key={x.id} className="resource-card"><span>{String(i+1).padStart(2,'0')}</span><div><p className="kicker">{String(x.type).replaceAll('_',' ')}</p><h3>{x.title}</h3><p>{x.description}</p></div><b>↗</b></a>)}</div></section></>}
